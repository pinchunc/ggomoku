library(tidyverse)

# Draws the board
gomoku_board <- function(size, color) {
    require(ggplot2)

    # Choose whether the board is 15x15 or 19x19


}


# Places a piece
gomoku_move <- function(x, y, color) {

    # checking missing arguments
    if (missing(x))
        stop("Need to specify x-coordinate of the move.")

    if (missing(y))
        stop("Need to specify y-coordinate of the move.")

    }

# Checks for victory
gomuku_victory <- function() {

    if (missing())

    paste0(color, "has won.", sep = "")

    }




